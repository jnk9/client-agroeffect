<header class="header black-bg">
  <% if user_signed_in? %>
    <div class="sidebar-toggle-box">
      <div class="fa fa-bars tooltips" data-placement="right" data-original-title="Toggle Navigation"></div>
    </div>
  <% end %>

  <a href="/" class="logo"><b>AgroEffect</b></a>

  <div class="top-menu">
    <ul class="nav pull-right top-menu">
      <% if user_signed_in? %>
        <li><%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete, class: 'logout'%></li>
      <% end %>
    </ul>
  </div>
</header>
